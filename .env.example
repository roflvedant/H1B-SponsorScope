# JSearch credential used by fetch_main.py and the POST /search endpoint.
JSEARCH_API_KEY=replace_with_your_jsearch_api_key

# JSearch endpoint. Most users can leave this unchanged.
JSEARCH_URL=https://api.openwebninja.com/jsearch/search-v2

# Optional in AWS: immutable raw JSearch snapshots are uploaded to this bucket.
# RAW_SNAPSHOT_BUCKET=sponsorscope-raw-example

# Local Docker PostgreSQL connection.
# Deployment will replace this with the hosted database URL.
DATABASE_URL=postgresql+psycopg://h1b_user:h1b_password@localhost:5432/h1b_jobs

# Browser origins permitted to call the API (comma-separated).
CORS_ALLOWED_ORIGINS=http://localhost:3000

# Optional bounded Amazon Bedrock review for deterministic UNKNOWN postings.
# Keep disabled until the ECS task role has bedrock:InvokeModel permission.
SPONSORSHIP_AGENT_ENABLED=false
BEDROCK_REGION=us-east-2
BEDROCK_MODEL_ID=amazon.nova-lite-v1:0
SPONSORSHIP_AGENT_MIN_CONFIDENCE=0.85
SPONSORSHIP_AGENT_MAX_DESCRIPTION_CHARS=12000
SPONSORSHIP_AGENT_MAX_REVIEWS_PER_SEARCH=20
SPONSORSHIP_AGENT_WORKERS=4

# Used only to estimate and log per-review cost. Update when Bedrock pricing
# changes, or set both values to zero to disable estimates.
BEDROCK_INPUT_COST_PER_MILLION_USD=0.06
BEDROCK_OUTPUT_COST_PER_MILLION_USD=0.24

# Optional custom data directory.
# Leave this commented to use <project-root>/data.
# DATA_DIRECTORY=C:/path/to/h1b_project/data
